# EarlyBird Collective Component Configuration

# This component has no special dependencies.
# We just need to define the configuration macro that OPAL_MCA expects.

AC_DEFUN([MCA_ompi_coll_earlybird_CONFIG], [
    should_build=1
])
